import { OpeningHoursPeriod } from './opening-hours-period';

describe('OpeningHoursPeriod', () => {
  it('should create an instance', () => {
    expect(new OpeningHoursPeriod()).toBeTruthy();
  });
});
